---
interface Props {
  currentPage?: string;
}

const { currentPage } = Astro.props;
---

<nav class="bg-gray-900 text-white shadow-lg sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo/Brand -->
      <div class="flex-shrink-0">
        <a href="/" class="text-2xl font-bold hover:opacity-80 transition-opacity duration-300 flex items-baseline gap-0">
            <span class="bg-gradient-to-r from-blue-400 to-cyan-300 bg-clip-text text-transparent">tquinonero</span><span class="text-blue-400 font-light">.dev</span>
          </a>
      </div>

      <!-- Navigation Links -->
      <div class="flex space-x-1">
        <a
          href="/"
          class={`px-4 py-2 rounded-lg font-medium transition-all duration-300 ${
            currentPage === 'index'
              ? 'bg-blue-600 text-white'
              : 'text-gray-300 hover:bg-gray-800 hover:text-white'
          }`}
        >
          Home
        </a>
        <a
          href="/about"
          class={`px-4 py-2 rounded-lg font-medium transition-all duration-300 ${
            currentPage === 'about'
              ? 'bg-blue-600 text-white'
              : 'text-gray-300 hover:bg-gray-800 hover:text-white'
          }`}
        >
          About
        </a>
        
        <a
          href="/blog"
          class={`px-4 py-2 rounded-lg font-medium transition-all duration-300 ${
            currentPage === 'blog'
              ? 'bg-blue-600 text-white'
              : 'text-gray-300 hover:bg-gray-800 hover:text-white'
          }`}
        >
          Blog
        </a>

        <!-- Tools Dropdown -->
        <div class="group relative">
          <button
            class={`px-4 py-2 rounded-lg font-medium transition-all duration-300 flex items-center gap-1 ${
              ['frontmatter-generator', 'wordpress-quiz', 'regex-tester', 'color-palette-generator', 'cron-expression-builder', 'robots-txt-generator'].includes(currentPage || '')
                ? 'bg-blue-600 text-white'
                : 'text-gray-300 hover:bg-gray-800 hover:text-white'
            }`}
          >
            <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75a4.5 4.5 0 0 1-4.884 4.484c-1.076-.091-2.264.071-2.95.904l-7.152 8.684a2.548 2.548 0 1 1-3.586-3.586l8.684-7.152c.833-.686.995-1.874.904-2.95a4.5 4.5 0 0 1 6.336-4.486l-3.276 3.276a3.004 3.004 0 0 0 2.25 2.25l3.276-3.276c.256.565.398 1.192.398 1.852Z" />
            </svg>
            Free Tools
            <svg class="w-3.5 h-3.5 shrink-0 transition-transform group-hover:rotate-180" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>

          <!-- Dropdown Menu -->
          <div class="absolute left-0 mt-0 w-56 bg-gray-800 rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 py-2 z-50">
            <a
              href="/frontmatter-generator"
              class={`block px-4 py-2 font-medium transition-colors duration-200 ${
                currentPage === 'frontmatter-generator'
                  ? 'bg-blue-600 text-white'
                  : 'text-gray-300 hover:bg-gray-700 hover:text-white'
              }`}
            >
              Frontmatter Generator
            </a>
            <a
              href="/wordpress-quiz"
              class={`block px-4 py-2 font-medium transition-colors duration-200 ${
                currentPage === 'wordpress-quiz'
                  ? 'bg-blue-600 text-white'
                  : 'text-gray-300 hover:bg-gray-700 hover:text-white'
              }`}
            >
              WordPress Quiz
            </a>
            <a
              href="/regex-tester"
              class={`block px-4 py-2 font-medium transition-colors duration-200 ${
                currentPage === 'regex-tester'
                  ? 'bg-blue-600 text-white'
                  : 'text-gray-300 hover:bg-gray-700 hover:text-white'
              }`}
            >
              Regex Tester
            </a>
            <a
              href="/color-palette-generator"
              class={`block px-4 py-2 font-medium transition-colors duration-200 ${
                currentPage === 'color-palette-generator'
                  ? 'bg-blue-600 text-white'
                  : 'text-gray-300 hover:bg-gray-700 hover:text-white'
              }`}
            >
              Color Palette Generator
            </a>
            <a
              href="/cron-expression-builder"
              class={`block px-4 py-2 font-medium transition-colors duration-200 ${
                currentPage === 'cron-expression-builder'
                  ? 'bg-blue-600 text-white'
                  : 'text-gray-300 hover:bg-gray-700 hover:text-white'
              }`}
            >
              Cron Expression Builder
            </a>
            <a
              href="/robots-txt-generator"
              class={`block px-4 py-2 font-medium transition-colors duration-200 ${
                currentPage === 'robots-txt-generator'
                  ? 'bg-blue-600 text-white'
                  : 'text-gray-300 hover:bg-gray-700 hover:text-white'
              }`}
            >
              Robots.txt Generator
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>