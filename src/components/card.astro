---
import { Image } from 'astro:assets';
import maintenance from '../maintenance.jpg';

interface Props {
  title: string;
  subtitle: string;
  content: string;
}

const { title, subtitle, content } = Astro.props;
---

<div class="bg-white border border-black shadow-lg p-6 rounded-lg relative overflow-hidden group">
  <!-- Background Image -->
  <div class="absolute inset-0 bg-cover bg-center bg-no-repeat opacity-15 group-hover:opacity-10 transition-opacity duration-300" style={`background-image: url(${maintenance.src}); background-size: cover; background-position: center;`}></div>
  
  <!-- Content -->
  <div class="relative z-10">
    <h2 class="text-black text-2xl font-bold mb-2">{title}</h2>
    <h3 class="text-black text-lg font-semibold mb-4">{subtitle}</h3>
    <p class="text-black">{content}</p>
  </div>
</div>